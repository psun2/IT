// 규칙은 바로 변수를 초기화하기 전에 이 단어를 넣어야 합니다. => let
let a = 221;
// let 을 처음에 써주어야 합니다.
let b = a - 5;

// let b 에 a 에는 왜 let 을 써주지 않나요 ?
// 변수를 초기화 하거나 생성할 때에는 let을 써야하지만,
// b 에서는 단순히 a를 사용 하기 때문에 이미 생성된 것을 사용 할 때는
// 단지 참조만 하면 됩니다.
